import { utils } from "ethers";
import * as yup from "yup"
import jaLocale from './ja-locale'

export const ja = jaLocale

/* eslint import/no-anonymous-default-export: [2, {"allowObject": true}] */
export default {
  ja,
  yup
}